[lint]
select = [
    "E",   # pycodestyle errors
    "F",   # pyflakes — catches undefined names, unused imports
    "I",   # isort — import ordering
]
ignore = ["E501"]  # line length not enforced — prompt strings are intentionally long

[lint.per-file-ignores]
"tests/*" = ["E501"]
